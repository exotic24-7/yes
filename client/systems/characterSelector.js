class CharacterSelector {
    constructor(){
        this.x = 0;
        this.y = 0;
        this.bounds = { x: 0, y: 0, w: canvas.w, h: canvas.h };
        this.hovering = false;
    }
    draw(){
        // placeholder
    }
}

window.characterSelector = new CharacterSelector();
